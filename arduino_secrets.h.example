// Copy to arduino_secrets.h and fill in. Must be the same 2.4 GHz network the
// Nesso N1 joins — the Wi-Fi transports (UDP:8889 / TCP:8890) need both ends on it.
// Irrelevant to the BLE transport, but the file is still required to compile.
#define SECRET_SSID "your-ssid"
#define SECRET_PASS "your-password"
