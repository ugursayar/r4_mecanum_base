# Arduino build output
build/

# Wi-Fi credentials — arduino_secrets.h.example is the tracked template
arduino_secrets.h

# Session memory / tooling scratch
.remember/
ruvector.db
**/ruvector.db

# Claude Code notes (local only)
CLAUDE.md

# Machine-specific IntelliSense config (absolute toolchain paths)
.vscode/c_cpp_properties.json
